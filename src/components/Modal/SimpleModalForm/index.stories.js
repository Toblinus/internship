import React from 'react';
import SMF from './index';

export default {
    title: 'Trello/Modal/SimpleForm',
    component: SMF
}

const Template = (args) => <SMF {...args} />;

export const Default = Template.bind({});